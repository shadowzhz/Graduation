已完成性能根因定位。

现在只做一个最小修改，不要重构其他代码。

修改：
air_hockey/camera/gst_backend.py

当前：

frame = np.frombuffer(map_info.data, dtype=np.uint8).reshape((height, width, 4))
return True, frame[:, :, :3].copy()

改为：

frame = np.frombuffer(map_info.data, dtype=np.uint8).reshape((height, width, 4))
return True, cv2.cvtColor(frame, cv2.COLOR_BGRA2BGR)

原因：
GStreamer 输出 BGRx。
实测同一 live buffer：
map ≈ 198 FPS
bgrx-copy ≈ 197 FPS
[:, :, :3].copy() ≈ 67~83 FPS
cv2.cvtColor(BGRA2BGR) ≈ 198 FPS

要求：
1. 只修改这一处性能路径。
2. 不修改 GStreamer pipeline。
3. 不修改 CameraManager。
4. 不修改 nvpmodel 或系统配置。
5. 不做额外防御性编程。
6. 修改后运行现有测试。
7. 运行：
   python3 air_hockey/tools/test_camera.py --benchmark --backend gstreamer --duration 10
8. 连续测试 3 次，记录 current / average / max FPS。
9. 不自动提交 git，测试结果先汇报。
