单行修改完成，未提交 git。

  修改内容（air_hockey/camera/gst_backend.py，唯一改动）：

  -            return True, frame[:, :, :3].copy()
    +            return True, cv2.cvtColor(frame, cv2.COLOR_BGRA2BGR)

  现有测试：python3 tests/run_tests.py → passed=53, failed=0

  基准测试（--backend gstreamer --duration 10，连续 3 次）

  ┌──────┬─────────┬─────────┬───────┬────────┐
  │ 运行 │ current │ average │ max   │ frames │
  ├──────┼─────────┼─────────┼───────┼────────┤
  │ 1    │ 197.8   │ 197.8   │ 197.8 │ 1988   │
  ├──────┼─────────┼─────────┼───────┼────────┤
  │ 2    │ 197.7   │ 197.7   │ 197.7 │ 1986   │
  ├──────┼─────────┼─────────┼───────┼────────┤
  │ 3    │ 198.1   │ 198.1   │ 198.1 │ 1988   │
  └──────┴─────────┴─────────┴───────┴────────┘

  对比修改前：56 ~ 80.6 FPS（波动）→ 现在稳定 197.7~198.1 FPS，已到
  200fps 模式的实际上限。

  验证：

  - 帧仍为正确 BGR 语义（cv2.COLOR_BGRA2BGR 丢弃 alpha、保留 BG
  R），shape=(720,1280,3) dtype=uint8 C-contiguous 
  writable=True，下游无需改动。
  - 未触碰
  pipeline、CameraManager、nvpmodel/系统配置，无新增防御性代码。
  - 未执行 git commit，改动仅停留工作区（git diff 只有这一行）。

