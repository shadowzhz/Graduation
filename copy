目标：解除 GUI 渲染对实时视觉处理线程的限速。

当前已确认：
- Camera ≈198 FPS
- headless 检测到冰壶后仍 >100 FPS
- GUI 检测到冰壶后只有约50~90 FPS
- preview_fps 默认只有20 FPS
- 当前 processing_loop 每处理一帧都会调用 render()，
  但大量渲染帧最终不会被 GUI 显示。

任务：

1. processing_loop 只执行：
   camera latest frame
   -> runtime.process_frame()
   -> 保存最新 VisionResult 和状态信息。

2. 不再在 processing_loop 中调用 render()。

3. 将 render() 移到现有 preview/encoding 线程。

4. preview/encoding 线程继续严格按 args.preview_fps 运行：
   最新 VisionResult
   -> render()
   -> resize
   -> BGR->RGB
   -> PPM
   -> shared["ppm"]

5. 只保留最新结果，不建立队列，不积压旧帧。

6. VisionRuntime、Detector、Tracker、Predictor、AI、
   CameraManager 均不修改。

7. 保持 GUI 显示内容和视觉效果不变。

8. 不新增复杂防御性代码。

9. 更新或补充必要测试。

10. 完成后运行：
    python3 tests/run_tests.py

11. 再分别测试：
    python3 main.py --headless --disable-homography
    python3 main.py --disable-homography

记录：
- 无冰壶处理 FPS
- 有冰壶处理 FPS
- 采集 FPS
- preview FPS

先汇报结果，不提交 git。
